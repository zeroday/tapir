<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
  </tr>

<% @tasks.each do |task| %>
  <tr>
    <td><%= link_to task.pretty_name, tapir_task_path(task.name) %></td>
    <td><%= task.description %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New task', new_tapir_task_path %>

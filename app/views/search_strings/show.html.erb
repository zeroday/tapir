<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @search_string.name %>
</p>

<p>
<b>Parents:</b>
  <ul>
  <% @search_string.parents.each {|p|%>
    <li><%= link_to p, eval("tapir_entities_#{p.underscore}_path(p)")  %></li>
  <% } %>
  </ul>
</p>  

<p>
<b>Children:</b>
  <ul>
  <% @search_string.children.each {|c|%>
    <li><%= link_to c, eval("tapir_entities_#{c.underscore}_path(c)")  %></li>
  <% } %>
  </ul>
</p>

<p>
<b>Tasks:</b>
  <ul>
  <% @search_string.tasks.each {|t|%>
    <li><%= link_to t, eval("tapir_#{t.underscore}_path(t.name)")  %></li>
  <% } %>
  </ul>
</p>

<%= link_to 'Edit', edit_tapir_entities_search_string_path(@search_string) %> |
<%= link_to "Destroy", @search_string, :confirm => 'Are you sure?', :method => :delete %> |
<%= link_to 'Back', tapir_entities_search_strings_path %>

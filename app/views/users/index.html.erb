<h1>Listing users</h1>

<table>
  <tr>
    <th>First name</th>
    <th>Middle name</th>
    <th>Last name</th>
    <th>Usernames</th>
    <th>Organization</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @users.each do |user| %>
  <tr>
    <td><%= user.first_name %></td>
    <td><%= user.middle_name %></td>
    <td><%= user.last_name %></td>
    <td>
      <ul>
      <% user.usernames.each do |u| %>
        <li> <%= u %> </li>
     <% end %>
     </ul>
   </td>
    <td><%= user.organization_id %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_tapir_entities_user_path %>
